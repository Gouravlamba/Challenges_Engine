import 'package:flutter/material.dart';

class InviteFriendsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Invite friends screen (placeholder)'),
    );
  }
}
